// Shared types for asset components

export interface AssetData {
  symbol: string;
  name: string;
  price: string;
  change: string;
  isPositive: boolean;
  logo: string;
  description: string;
}
